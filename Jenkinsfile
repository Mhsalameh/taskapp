pipeline {
    
    agent any

    stages {

        stage("build") {

            steps {
                echo 'building application ...'
                echo 'testing polls'
            }
        }

        stage("test") {

            steps {
                echo 'testing application ...'
            }
        }

        stage("deploy") {

            steps {
                echo 'deploying application ...'
            }
        }
    }
}
